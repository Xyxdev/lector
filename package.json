{
  "name": "rez-lector",
  "version": "1.0.0",
  "description": "Rez Lector — lector RSVP de libros para Android",
  "private": true,
  "scripts": {
    "test": "node tests/rsvp-engine.test.js",
    "prepare:www": "node scripts/copy-jszip.js",
    "sync": "npm run android:sync",
    "android:sync": "npm run prepare:www && npx cap sync android",
    "android:debug": "npm run android:sync && cd android && gradlew assembleDebug",
    "android:bundle": "npm run android:sync && cd android && gradlew bundleRelease",
    "android:open": "npx cap open android",
    "build:android": "npm run android:debug"
  },
  "dependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/core": "^8.0.0",
    "jszip": "^3.10.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0",
    "@capacitor/assets": "^3.0.5"
  }
}
