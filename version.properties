# Android app version for Play Store builds.
#
# Increase VERSION_CODE for every Play Console upload. VERSION_NAME is the
# user-visible label. The Android project is committed, but CI still reads this
# file so Gradle and Play Console releases stay in sync.

VERSION_CODE=1
VERSION_NAME=1.0.0
